cat：如果文件数据量大，则只能看前面一部分
	-n	显示行号
	-b	显示非空行号
	-s	合并空白行
	-E	在每行结尾添加$
	-v	显示不可打印字符
	-T	将制表符显示为^|
more：可以逐行浏览（回车）或者翻页（空格），但只能往后不能往前
less：可以逐行浏览（Ctrl + p == 上一行，Ctrl + n == 下一行）或者翻页（Ctrl + b == 上一页，Ctrl + f == 下一页）
head -前多少行
tail -后多少行