**gzip压缩**
    命令：gzip 文件
        1 会删除原文件；
        2 不会将所有要压缩的文件打包到一起

**gunzip解压**
    命令：gunzip 文件.gz


**bzip2压缩**
    命令：bzip2 [-k] 文件
        1 -k：保留原文件
        2 不会将所有要压缩的文件打包到一起

**bunzip2解压**
    命令：bunzip2 文件.bz2


_注意_：这两个屌丝命令除了使用不便外，还不能操作目录。