ulimit -a：显示当前进程中所有资源限制的当前值。
ulimit -c：设置或显示核心文件的最大大小（以字节为单位）。
**ulimit -n**：设置或显示可以打开的文件描述符的最大数量。
ulimit -u：设置或显示可以创建的最大用户进程数。
ulimit -m：设置或显示可用的最大内存大小（以字节为单位）。
ulimit -s：设置或显示栈的最大大小（以字节为单位）。
ulimit -f：设置或显示可创建的最大文件大小（以字节为单位）。
ulimit -r：设置或显示最大实时优先级。
ulimit -t：设置或显示最大 CPU 时间（以秒为单位）。