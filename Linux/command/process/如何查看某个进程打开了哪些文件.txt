sudo lsof [-*]
-*:
-p <PID>
列出指定进程 ID (PID) 打开的文件。

-i:
列出网络连接信息。

-c <command>
列出指定命令相关的打开文件。

-u <user>
列出指定用户打开的文件。

-t
仅显示进程 ID。

-n
不解析主机名。



其中最有用的就是-i，比如你知道某个进程使用了1121这个端口号，想杀死这个进程，就需要知道这个进程的pid，此时就可以使用命令：
sudo lsof -i :1121 -t   （格式是：[protocol:port]，protocol可以忽略，和i之间可以有空格也可以没有）
来获取该pid