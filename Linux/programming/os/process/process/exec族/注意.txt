1 比起之前我们在分支结构里直接写子进程代码，在分支结构里调用exec族函数更加清晰，但是要注意二者间的不同点：前者执行完后会接着执行一些公共部分代码，后者一旦执行不再返回；

2 调用exec并不创建新进程，你可以理解为exec函数将分支结构里的代码封装到了另一个文件的main函数里，所以调用exec前后pid并未改变；

3 事实上，只有execve是真正的系统API，其他五个函数最终都调用execve。