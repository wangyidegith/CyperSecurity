#include <unistd.h>
int pipe(int fd[2]);

返回值：成0败-1
参数：fd数组是一个传出参数，负责接收读写文件描述符，fd[0]是读端，fd[1]是写端

在使用pipe函数时，要注意三点：
①管道是半双工的，这一点对于命名管道来讲也是这样，②③是匿名管道不同于命名管道的地方
半双工意即假设A与B通信，要么A是读端B是写端，要么A是写端B是读端，A和B都不能同时打开着读端和写端而只能打开一个
②匿名管道只能用于有血缘关系的父子进程间的通信
③匿名管道充分地体现了”Linux下一切皆文件“，匿名管道本质上是内核中的一块缓冲区，它位于内存而非硬盘上