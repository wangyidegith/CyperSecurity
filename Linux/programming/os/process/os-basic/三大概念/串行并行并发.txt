串行：一条线路，依次传输。
	网络的传输就是串行的，比如要传输10011100，一个接一个的传；

并行：多条线路，同时传输。
	计算机内部的通信是并行的，假设我们现在有8条线路，第一条传输1，第二条传输0......每条传输一个比特；
	再比如有3个CPU，每个CPU执行一个进程；

并发（虚拟并行）：一条线路，”同时“传输。
	一个CPU，并发执行3个进程——这个方法有很多，其中一种是通过给每个进程分配时间片，A进程执行一会，B进程再执行一会这样，由于CPU的执行速度远大于人眼的观测速度，所以人觉得是并行的。