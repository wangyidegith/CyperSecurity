只读存储器：这是一块特殊的内存

在制造ROM的时候，信息（数据或程序）就被存入并永久保存。

这些信息只能读出，一般不能写入，即使机器停电，这些数据也不会丢失。

ROM一般用于存放计算机的基本程序和数据，如BIOS ROM。

现在比较流行的只读存储器是闪存(Flash Memory)。

U盘和固态硬盘(SSD)也是利用闪存原理做成的。

ROM用来存储固件即指令系统，也就是说ROM是软件硬件之间的桥梁。
