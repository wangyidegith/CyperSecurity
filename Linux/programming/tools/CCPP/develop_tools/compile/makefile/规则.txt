目标（命令生成的文件或者目录）：依赖（需要用到的文件）
	（前面是一个Tab）命令（可以是任意命令，不是说只能是gcc）

以上就是一个makefile文件的基本单元的格式，可以写多个这样的基本格式，我们也可以写一个下面这种情况的makefile：
	.PHONY:clean（表示clean是一个伪目标，即这个目标并不会生成，这样做的意义在于避免与该目录下的同名文件产生冲突）
	clean:（伪目标名，这里并不需要写依赖）
	        [-]rm    $(obj)    $(target)（这就是命令了，-表示如果执行失败接着执行，如果没有-，执行失败则makefile结束）